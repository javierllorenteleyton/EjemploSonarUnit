<?xml version="1.0" encoding="UTF-8" ?>
<!--Ant buildfile generated by Oracle JDeveloper-->
<!--Generated Mar 7, 2017 7:04:40 PM-->
<project name="ModelTest" default="jacoco">
  <import file="./buildTest.xml"/>
 
  <target name="jacoco.init">
  <mkdir dir="${jacoco.dir}"/>
<mkdir dir="${jacoco.class.dir}"/>
<mkdir dir="${jacoco.html.dir}"/>
<mkdir dir="${jacoco.csv.dir}"/>
<mkdir dir="${jacoco.xml.dir}"/>
<mkdir dir="${jacoco.junit.xml.dir}"/>
<mkdir dir="${jacoco.junit.html.dir}"/>
<mkdir dir="${jacoco.junit.pdf.dir}"/>
  </target>
  <target name="jacoco" depends="jacoco.init,test.all"
          description="Run unit tests and code coverage reporting">
    <jacoco:coverage destfile="${jacoco.dir}/jacoco.exec"
                     xmlns:jacoco="antlib:org.jacoco.ant">
      <junit printsummary="true" fork="yes" failureproperty="test.failed"
             forkmode="once">
        <jvmarg value="${xms}"/>
        <jvmarg value="${xmx}"/>
        <classpath refid="classpath.jacoco.test"/>
        <formatter type="xml"/>
        <batchtest todir="${jacoco.junit.xml.dir}">
          <fileset dir="./classes">
            <include name="**/*Test.class"/>
          </fileset>
        </batchtest>
      </junit>
    </jacoco:coverage>
    <junitreport todir="${jacoco.dir}/junit">
      <fileset dir="${jacoco.junit.xml.dir}">
        <include name="TEST-*.xml"/>
      </fileset>
      <report format="frames" todir="${jacoco.junit.html.dir}"/>
      <report styledir="${jacoco.junit.style.dir}" format="noframes"
              todir="${jacoco.junit.html.dir}2"/>
      <report styledir="${jacoco.junit.style.dir}" format="frames"
              todir="${jacoco.junit.html.dir}3"/>
    </junitreport>
    <junitpdfreport todir="${jacoco.junit.pdf.dir}"
                    styledir="${jacoco.junit.style.pdf.dir}">
      <fileset dir="${jacoco.junit.xml.dir}">
        <include name="TEST-*.xml"/>
      </fileset>
    </junitpdfreport>
    <jacoco:report xmlns:jacoco="antlib:org.jacoco.ant">
      <executiondata>
        <file file="${jacoco.dir}/jacoco.exec"/>
      </executiondata>
      <structure name="jacoco-sample">
        <classfiles>
          <fileset dir="../Model/classes"/>
        </classfiles>
        <sourcefiles encoding="UTF-8">
          <fileset dir="../Model/src"/>
        </sourcefiles>
      </structure>
      <html destdir="${jacoco.html.dir}"/>
      <csv destfile="${jacoco.csv.dir}/report.csv"/>
      <xml destfile="${jacoco.xml.dir}/report.xml"/>
    </jacoco:report>
  </target>
</project>
